# OpenCode Delivery Overlay

The adapter-neutral delivery contract lives at `.lufy/contracts/delivery.md` and MUST be read completely before delivery work.

OpenCode execution details remain in `.opencode/agents/delivery.md`. This overlay exists only for compatibility with older OpenCode prompts and links; it MUST NOT redefine or override the shared contract.
